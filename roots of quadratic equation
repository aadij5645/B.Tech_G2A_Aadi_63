#include<stdio.h>
#include<math.h>
int main()
{
    int a,b,c,r1,r2;
    printf("Enter the vaues of a,b,c of a quadratic equation\n");
    scanf("%d%d%d",&a,&b,&c);
    r1=(-b+sqrt(b*b-4*a*c))/(2*a);
    r2=(-b-sqrt(b*b-4*a*c))/(2*a);
    printf("The roots of the quadratic equation are %d and %d",r1,r2);
    return 0;    
}
